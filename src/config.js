const BASE_URL = 'https://api.github.com';

export const TOKEN = process.env.REACT_APP_TOKEN;

export const REPO_INFO_API = `${BASE_URL}/repos`;

export const REPOS_SEARCH_API = `${BASE_URL}/search`;
